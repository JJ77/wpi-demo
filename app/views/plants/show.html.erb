<p id="notice"><%= notice %></p>

<p>
  <strong>Item code:</strong>
  <%= @plant.item_code %>
</p>

<p>
  <strong>Description:</strong>
  <%= @plant.description %>
</p>

<p>
  <strong>Parent plant:</strong>
  <%= @plant.parent_plant_id.nil? ? "None" : Plant.find(@plant.parent_plant_id).description %>
</p>

<%= link_to 'Edit', edit_plant_path(@plant) %> |
<%= link_to 'Back', plants_path %>
